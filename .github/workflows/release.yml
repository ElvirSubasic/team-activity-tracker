name: Manual releases only

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Manual release notice
        run: |
          echo "GitHub Actions releases are disabled for this repository."
          echo "Use the manual release process documented in README.md (Section 14.1)."
